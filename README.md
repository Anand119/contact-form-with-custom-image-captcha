# contact-form-with-custom-image-captcha
To create a contact form with custom image captcha validation, you can use a WordPress plugin like Image CAPTCHA for Contact Form 7 and WPForms for easy implementation or build custom solution using PHP where a PHP generates a random image stores it in a session, and then validates the user's input against the session variable on form submission. 
